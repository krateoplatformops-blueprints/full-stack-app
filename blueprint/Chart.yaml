apiVersion: v2
name: full-stack-app
description: >
  This is a blueprint used to scaffold full-stack app
  This blueprint is composed by:
  - A CNPG cluster (database)
  - A Redis cluster (cache)
  - A Deployment of a simple backend service that connects to the database and the cache
  - A Deployment of a simple frontend service that connects to the backend

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: CHART_VERSION

home: https://krateo.io
icon: "https://github.com/krateoplatformops/krateo/blob/main/docs/media/logo.svg"
keywords:
  - operations
  - portal
sources:
  - https://github.com/krateoplatformops-blueprints/full-stack-app
annotations:
  krateoSupportedVersion: "3.0.0"

dependencies:
- name: portal-composition-page-generic
  version: "1.4.3"
  repository: "https://marketplace.krateo.io"
- name: full-stack-app-portal
  version: "1.0.8"
  repository: "https://marketplace.krateo.io"
  condition: portal.enabled
