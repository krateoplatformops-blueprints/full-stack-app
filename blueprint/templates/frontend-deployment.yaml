apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-frontend
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-frontend
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-frontend
    spec:
      containers:
        - name: frontend
          image: "{{ .Values.app.frontend.image.repository }}:{{ .Values.app.frontend.image.tag }}"
          imagePullPolicy: Always
          env:
            - name: BACKEND_HOST
              value: "{{ .Release.Name }}-backend"
            - name: BACKEND_PORT
              value: {{ .Values.app.backend.port | quote }}
          ports:
            - containerPort: {{ .Values.app.frontend.port }}
          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.app.frontend.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
